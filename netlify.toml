[build]
  command = "npm test && npm start"
  publish = "build/"

[build.environment]
  # FONTAWESOME_TOKEN
  # UNSPLASH_ACCESS_KEY
  # UNSPLASH_SECRET_KEY
  NODE_ENV = "production"
  TZ = "America/Los_Angeles"

[build.processing]
  # Site settings > Build & deploy > Post processing > Asset Optimization
  skip_processing = false

[[context.production.plugins]]
  package = "netlify-plugin-submit-sitemap"

[[context.deploy-preview.plugins]]
  package = "@netlify/plugin-lighthouse"
  [context.deploy-preview.plugins.inputs]
    output_path = "build/lighthouse/index.html"
  [[context.deploy-preview.plugins.inputs.audits]]
    path = "blog/starting-a-metalsmith-project"
    output_path = "build/lighthouse/blog.html"
