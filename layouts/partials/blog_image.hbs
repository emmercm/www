{{#isObject image}}
{{else}}
    <img src="{{image}}" alt="{{sanitize title}}" class="w-100">
    {{#if imageCredit}}
        <p class="mt-3 text-center font-italic">{{{ imageCredit }}}</p>
    {{/if}}
{{/isObject}}
